spring:
  application:
    name: history-app
  profiles:
    active: local
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
info:
  artifactId: '@project.artifactId@'
  version: '@project.version@'